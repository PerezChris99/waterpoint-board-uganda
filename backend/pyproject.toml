[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["migrations"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "ARG"]
ignore = ["ARG001"]

[tool.pytest.ini_options]
testpaths = ["tests"]
